language: node_js

notifications:
  email: false

node_js:
  - "12.13.1"

services:
  - postgresql

before_script:
  - psql -c "CREATE DATABASE retyp-testing;" -U postgres
  - psql -c "CREATE USER retyp WITH PASSWORD 'password';" -U postgres

install: npm install

jobs:
  include:
    - stage: Tests
      if: |
        branch = develop AND \
        tag IS blank AND \
        commit_message !~ /(no-deploy|wip)/
      script: 
        - npm run lint
        - npm run test

cache:
  directories:
    - node_modules